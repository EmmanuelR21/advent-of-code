#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>

//53651

int main() {
  FILE *fptr;
  fptr = fopen("d1Input", "r");
  int sum = 0;

  if (fptr != NULL) {
  }

  printf("Total sum is: %d \n", sum);

  return 0;
}
